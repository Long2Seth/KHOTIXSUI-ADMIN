export type User = {
    id: string
    user: string
    username: string
    email: string
    phoneNumber: string
    status: "enabled" | "disabled"
}

export type Organizer = {
    id: string
    user: string
    username: string
    email: string
    phoneNumber: string
    status: "enabled" | "disabled"
}